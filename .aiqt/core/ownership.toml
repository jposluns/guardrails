# .aiqt/core/ownership.toml: the ANCHORED CANONICAL OWNERSHIP MAP (VER-CORE 4.2).
#
# Concern 1 (release-file inventory): every git-tracked path in the candidate release commit, MINUS the
# concrete-literal [[exclusion]] set, carries EXACTLY ONE [[release-class]] below. Scope is computed BY
# INVERSION (all tracked paths minus the exclusions); there is NO inclusion hand-list and NO default
# class: an in-scope tracked path matching no selector fails the build loud (exit 2), prompting either an
# explicit class row or a concrete exclusion line. Selectors are an exact `path` OR a literal directory
# prefix `pattern` ending in `/**`; nothing else. Selectors must be DISJOINT: two selectors matching the
# same tracked path are a schema FAIL, never a precedence pick.
#
# Concern 2 ([[namespace-class]]): adopter-created namespaces with ZERO tracked entries here, exempt from
# the concern-1 NO-STRAYS leg, and the classification authority the adopter-side verifier uses.
#
# Concern 3: the adopter-installed extent is NOT rostered here; it is defined in the adopter-experience
# spec ([adopter-extent] below is the hook).
#
# Verified by tools/gen_manifest.py and tools/check_manifest.py (schema violations are fail-closed exit 2).

format-version = 1

# --- concern-1 concrete-literal exclusion set (the ONLY hand-maintained list; visible, reviewable) -----

[[exclusion]]
path = ".gitignore"
reason = "Repository-local development metadata; tracked but not shipped."

[[exclusion]]
path = ".github/workflows/currency.yml"
reason = "CI currency workflow; tracked, not shipped. Exact literal, not a glob (F-235)."

[[exclusion]]
path = ".github/workflows/quality.yml"
reason = "CI quality workflow; tracked, not shipped. Exact literal, not a glob (F-235)."

# --- concern-1: manifest-self and the managed block ----------------------------------------------------

[[release-class]]
path = ".aiqt/manifest.toml"
class = "manifest-self"
note = "Excluded from its own SOURCES (the fixed point); verified against the ROOT."

[[release-class]]
path = "CLAUDE.md"
class = "managed-block"
note = "Mixed surface: only the RULES-INDEX delimited block is pack-owned; bytes outside it are adopter-owned."

# --- concern-1: derived (recomputation-verified; outside the 4.3 set-equality walk) --------------------

[[release-class]]
path = ".aiqt/release/root.txt"
class = "derived"
note = "ROOT over the manifest bytes; carved out of SOURCES (R9-6); verified by recomputation."

[[release-class]]
path = ".aiqt/release/announce-snippet.txt"
class = "derived"
note = "Regenerated from manifest plus version; carved out of SOURCES (R9-6)."

[[release-class]]
pattern = "site/downloads/**"
class = "derived"
note = "Published outputs, corpus-derived, manifest-covered in SOURCES. The machine-generated outputs are byte-identity drift-gated at source by their own gensrc drift gates. The hand-authored sized instruction condensations (aiqt-instructions-8k/5k/1_5k.txt) have no generator; tools/check_sized_instructions.py gates their character caps, version agreement (rejecting a malformed version token such as a 4th segment or a prerelease/build suffix, and treating a malformed 'Version' line beside a valid one as a fail-closed finding), and a recorded SHA-256 of the generated source, but their editorial reconciliation with that source is a maintainer --update step whose git-diff check proves only that a condensation changed versus HEAD, not that the change preserved the meaning (a whitespace-only edit passes), so semantic fidelity rests on maintainer review, not on the gate. This gate, and the sibling tools/check_install_page.py tripwire over the install page, are static checks that catch ACCIDENTAL regression by a maintainer editing the site; they are NOT adversarial-grade. A determined author can still bypass a static check by construction (for the install page, a hidden ancestor or wrapper element, exotic inline CSS hiding such as opacity:0/height:0/transform:scale(0)/clip-path, a hidden status element, or a new unguarded rule; for --update, a git-evaluation error or a semantically empty edit that passes the git-diff heuristic). That residual class is unbounded and is disclosed, not chased closed."

# --- concern-1: pack-immutable subtrees -----------------------------------------------------------------

[[release-class]]
pattern = ".aiqt/core/**"
class = "pack-immutable"

[[release-class]]
pattern = ".aiqt/standards/**"
class = "pack-immutable"

[[release-class]]
pattern = ".claude/rules/aiqt/**"
class = "pack-immutable"

[[release-class]]
pattern = ".claude/rules/security/**"
class = "pack-immutable"

[[release-class]]
pattern = ".cursor/rules/aiqt-guardrails/**"
class = "pack-immutable"

[[release-class]]
pattern = "plugin/aiqt-guardrails-hooks/**"
class = "pack-immutable"

[[release-class]]
pattern = "tools/**"
class = "pack-immutable"
note = "Shipped pack tooling, byte-exact; explicitly NOT dev-only (Section 12 step 2)."

# --- concern-1: pack-immutable exact paths (.aiqt files, adapters, .claude/.cursor files) ---------------

[[release-class]]
path = ".aiqt/attribution.toml"
class = "pack-immutable"

[[release-class]]
path = ".aiqt/enforceability.json"
class = "pack-immutable"

[[release-class]]
path = ".aiqt/gensrc.json"
class = "pack-immutable"

[[release-class]]
path = ".aiqt/record-sections.toml.example"
class = "pack-immutable"
note = "Adopter example for tools/check_record_sections.py; deliberately NOT the gate's live default path (.aiqt/record-sections.toml), so the gate stays NOT APPLICABLE until an adopter opts in."

[[release-class]]
path = ".claude/RULES-INDEX.md"
class = "pack-immutable"

[[release-class]]
path = ".claude/rules/README.md"
class = "pack-immutable"
note = "Hand-authored shipped pack content, an explicit per-path entry (4.2)."

[[release-class]]
path = ".github/copilot-instructions.md"
class = "pack-immutable"

[[release-class]]
path = ".gitattributes"
class = "pack-immutable"
note = "Generated by gen_manifest.py from this map; carries its own class (bytes, not a digest of itself)."

# --- concern-1: pack-immutable root files (worked examples of the inversion, 4.2) -----------------------

[[release-class]]
path = "AGENTS.md"
class = "pack-immutable"

[[release-class]]
path = "CHANGELOG.md"
class = "pack-immutable"

[[release-class]]
path = "DISCLOSURE.md"
class = "pack-immutable"

[[release-class]]
path = "GEMINI.md"
class = "pack-immutable"

[[release-class]]
path = "LICENSE"
class = "pack-immutable"

[[release-class]]
path = "NOTICE"
class = "pack-immutable"

[[release-class]]
path = "README.md"
class = "pack-immutable"

[[release-class]]
path = "RELEASING.md"
class = "pack-immutable"

[[release-class]]
path = "ROADMAP.md"
class = "pack-immutable"

[[release-class]]
path = "SCOPE.md"
class = "pack-immutable"

[[release-class]]
path = "SYSTEM-HARDENING.md"
class = "pack-immutable"

[[release-class]]
path = "VERSION"
class = "pack-immutable"

[[release-class]]
path = "aiqt-barebones.md"
class = "pack-immutable"

[[release-class]]
path = "changelog.toml"
class = "pack-immutable"

[[release-class]]
path = "disclosure.toml"
class = "pack-immutable"

[[release-class]]
path = "roadmap.toml"
class = "pack-immutable"

# --- concern-1: pack-immutable non-download site paths (each literal; the block-generated pages
# --- site/disclosure.html, site/mappings.html, site/roadmap.html, site/install.html are whole-file
# --- pack-immutable here, their block FRESHNESS checked separately by each block generator's own --check, 4.2)

[[release-class]]
path = "site/404.html"
class = "pack-immutable"

[[release-class]]
path = "site/_redirects"
class = "pack-immutable"

[[release-class]]
path = "site/about.html"
class = "pack-immutable"

[[release-class]]
path = "site/development.html"
class = "pack-immutable"

[[release-class]]
path = "site/disclosure.html"
class = "pack-immutable"

[[release-class]]
path = "site/evidence.html"
class = "pack-immutable"

[[release-class]]
path = "site/examples.html"
class = "pack-immutable"

[[release-class]]
path = "site/favicon.svg"
class = "pack-immutable"

[[release-class]]
path = "site/index.html"
class = "pack-immutable"

[[release-class]]
path = "site/install.html"
class = "pack-immutable"

[[release-class]]
path = "site/learn.html"
class = "pack-immutable"

[[release-class]]
path = "site/mappings.html"
class = "pack-immutable"

[[release-class]]
path = "site/roadmap.html"
class = "pack-immutable"

[[release-class]]
path = "site/robots.txt"
class = "pack-immutable"

[[release-class]]
path = "site/rule1.html"
class = "pack-immutable"

[[release-class]]
path = "site/rule2.html"
class = "pack-immutable"

[[release-class]]
path = "site/rule3.html"
class = "pack-immutable"

[[release-class]]
path = "site/rule4.html"
class = "pack-immutable"

[[release-class]]
path = "site/rule5.html"
class = "pack-immutable"

[[release-class]]
path = "site/standard.html"
class = "pack-immutable"

[[release-class]]
path = "site/styles.css"
class = "pack-immutable"

[[release-class]]
path = "site/teams.html"
class = "pack-immutable"

[[release-class]]
path = "site/tech-details.html"
class = "pack-immutable"

[[release-class]]
path = "site/theme.js"
class = "pack-immutable"

[[release-class]]
path = "site/js/install.js"
class = "pack-immutable"

# --- concern-2: reserved-namespace / adopter-state patterns (ZERO tracked entries; NO-STRAYS exempt) ----

[[namespace-class]]
path = ".aiqt/derived-command-parameters.toml"
class = "adopter-state"
note = "Adopter-authored configuration for tools/check_derived_command_parameters.py; reserved, never a pack-shipped file (absent = gate NOT APPLICABLE)."

[[namespace-class]]
path = ".aiqt/pin.toml"
class = "adopter-state"

[[namespace-class]]
path = ".aiqt/pin-history.toml"
class = "adopter-state"

[[namespace-class]]
path = ".aiqt/pin-transition.toml"
class = "adopter-state"
note = "The two-phase re-pin transition record (10.3); prepared/applied/committed."

[[namespace-class]]
path = ".aiqt/pin-unadopt.toml"
class = "adopter-state"
note = "The un-adopt INTENT record (10.6); written before the reverse swap, removed last; recover completes an interrupted un-adopt."

[[namespace-class]]
pattern = ".aiqt/pin-preimages/**"
class = "adopter-state"
note = "Standalone re-pin and pre-adoption preimage payloads (10.3); no journal engine; retained until the pin advances again (10.6)."

[[namespace-class]]
pattern = ".aiqt/migration/**"
class = "adopter-state"
note = "Includes the crosswalk, repoints, and the cutover journal directory (7.4, 9.3)."

[[namespace-class]]
pattern = ".aiqt/archive/**"
class = "archive"
note = "Hash-pinned per 8.2; never manifest-listed."

[[namespace-class]]
pattern = ".claude/rules/external/**"
class = "adopter-state"
note = "The documented adopter vendoring location; reserved, never a pack-immutable set-equality member."

# --- checkout byte stability (3.3): every concern-1 path is `text eol=lf` except these ------------------

[checkout]
binary = ["site/downloads/aiqt-skill.zip", "site/downloads/aiqt-skill-1.0.3.zip"]

# --- concern-3 hook: the adopter-installed extent is defined in the adopter-experience spec -------------

[adopter-extent]
authority = "adopter-experience-spec"
